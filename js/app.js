'use strict'

let seattle = {
  minCustomer: 23,
  maxCustomer: 65,
  avgCookieCustomer: 6.3,
  function randomNum(min,max) {
    return Math.floor(Math.random() * (max - min + 1) + min);
  }
};

let tokyo = {
  minCustomer: 3,
  maxCustomer: 24,
  avgCookieCustomer: 1.2,
  function randomNum(min,max) {
    return Math.floor(Math.random() * (max - min + 1) + min);
  }
};

let dubai = {
  minCustomer: 11,
  maxCustomer: 38,
  avgCookieCustomer: 3.7,
  function randomNum(min,max) {
    return Math.floor(Math.random() * (max - min + 1) + min);
  }
};

let paris = {
  minCustomer: 20,
  maxCustomer: 38,
  avgCookieCustomer: 4.3,
  function randomNum(min,max) {
    return Math.floor(Math.random() * (max - min + 1) + min);
  }
};

let lima = {
  minCustomer: 2,
  maxCustomer: 16,
  avgCookieCustomer: 4.6,
  function randomNum(min,max) {
    return Math.floor(Math.random() * (max - min + 1) + min);
  }
};